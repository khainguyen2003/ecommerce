# C?u h�nh server v� ?ng d?ng
server:
  port: 9200

spring:
  application:
    name: ecommerce
    serverName: http://localhost:8080

  datasource:
    url: jdbc:postgresql://localhost:5433/ecommerce
    username: postgres
    password: 123456

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    hibernate:
      ddl-auto: update
    show-sql: true

  mail:
    from: khainv2k3@gmail.com
    host: smtp.gmail.com
    port: 587
    username: khainv2k3@gmail.com
    password: zdvnrqfjwabahdxr
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

  kafka:
    bootstrap-servers: localhost:9092,localhost:9093,localhost:9095
    admin:
      auto-create: true
    consumer:
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        session:
          timeout:
            ms: 15000
        spring.json.trusted.packages: '*'
      enable-auto-commit: true
      auto-offset-reset: earliest
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring:
          json:
            add.type.headers: true

endpoint:
  confirmUser: http://localhost:${server.port}/user/confirm